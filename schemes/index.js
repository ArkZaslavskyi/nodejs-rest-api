const {
  schemaPostContact,
  schemaPutContact,
  schemaPatchContactStatus,
} = require("./schemesContact");

const { schemaUser } = require("./schemaUser");

module.exports = {
  schemaPostContact,
  schemaPutContact,
  schemaPatchContactStatus,
  schemaUser,
};
